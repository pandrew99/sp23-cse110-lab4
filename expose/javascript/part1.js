function sumValues (num1, num2, add){
  
    if (add) {
      
      const result = 0; // var (q1,2), let (q3,4), const (q5,6)
      
      result = num1+num2;
      
      console.log('values added: ', result);
      
    } else return;
    
    console.log('final result: ', result);
  }
  
  sumValues(10,10,true);